# Phase 1 -- data spine, preprocessing, image-quality module.
# Later phases add to this file rather than replacing it (grad-cam, onnx,
# simpy, fastapi, weasyprint, etc. are not needed yet).

# Core numerics / CV
numpy
scipy
pandas
scikit-learn
scikit-image
opencv-python-headless

# Deep learning
torch
torchvision
timm
albumentations
segmentation-models-pytorch

# Config / experiment tracking
pyyaml
tensorboard
matplotlib

# Dataset access
kaggle

# Deployment / export
onnx
onnxruntime
onnxscript

# Explainability / reports
grad-cam
jinja2
playwright  # PDF rendering via headless print -- WeasyPrint's pango/cairo
            # deps don't load cleanly on this machine (see AGENTS.md); run
            # `playwright install chromium` once after installing this.

# Testing
pytest
